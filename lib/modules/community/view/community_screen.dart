import 'package:flutter/material.dart';

class CommnityScreen extends StatelessWidget {
  const CommnityScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('CommnityScreen'),
      ),
    );
  }
}
